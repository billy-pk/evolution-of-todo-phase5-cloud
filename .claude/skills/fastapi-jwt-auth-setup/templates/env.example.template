# JWT Authentication Configuration

# ===== Better Auth Configuration =====
# If using Better Auth (https://www.better-auth.com/)

# Frontend URL where Better Auth is running
BETTER_AUTH_URL=http://localhost:3000

# Shared secret for JWT validation
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-here-change-this-in-production

# ===== Auth0 Configuration =====
# If using Auth0 (https://auth0.com/)
# Uncomment these and comment out Better Auth settings above

# AUTH0_DOMAIN=your-tenant.auth0.com
# JWKS_URL=https://your-tenant.auth0.com/.well-known/jwks.json
# JWT_USER_ID_CLAIM=sub
# JWT_ALGORITHMS=["RS256"]

# ===== Clerk Configuration =====
# If using Clerk (https://clerk.com/)
# Uncomment these and comment out Better Auth settings above

# CLERK_DOMAIN=your-instance.clerk.accounts.dev
# JWKS_URL=https://your-instance.clerk.accounts.dev/.well-known/jwks.json
# JWT_USER_ID_CLAIM=sub
# JWT_ALGORITHMS=["RS256"]

# ===== Custom JWKS Provider =====
# If using a custom auth provider
# Uncomment and configure these settings

# JWKS_URL=https://your-auth-provider.com/.well-known/jwks.json
# JWT_USER_ID_CLAIM=user_id  # or "sub", "id", etc.
# JWT_ALGORITHMS=["EdDSA","ES256","RS256"]

# ===== Database Configuration =====
# Your database connection string
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# ===== API Configuration =====
API_HOST=0.0.0.0
API_PORT=8000
ENVIRONMENT=development

# ===== Database Connection Pooling =====
DB_POOL_SIZE=5
DB_POOL_MAX_OVERFLOW=10

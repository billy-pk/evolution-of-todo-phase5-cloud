# Note: Dapr 1.16+ automatically creates a 'kubernetes' secretstore component
# No need to define it explicitly - Dapr provides it by default
#
# Using native Kubernetes Secrets for local development
# Secrets are stored as Kubernetes Secret resources
# Example: kubectl create secret generic postgres-credentials --from-literal=connectionString="postgresql://..."
#
# Required Secrets:
# 1. postgres-credentials:
#    - connectionString: "postgresql://user:password@host:port/database"
# 2. openai-credentials (existing from Phase 3/4):
#    - apiKey: "sk-..."
# 3. better-auth-secret (existing from Phase 3/4):
#    - secret: "<shared-secret>"
#
# REMOVED: The explicit 'kubernetes' secretstores.kubernetes component
# Reason: Dapr 1.16.5 auto-creates this component, and defining it
# manually causes "component kubernetes already exists" error.

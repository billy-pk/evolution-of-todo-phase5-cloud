apiVersion: v1
kind: Secret
metadata:
  name: {{ include "websocket-service.fullname" . }}-secrets
  labels:
    {{- include "websocket-service.labels" . | nindent 4 }}
type: Opaque
data:
  # Database URL (base64 encoded)
  database-url: {{ .Values.env.DATABASE_URL | default "" | b64enc | quote }}
